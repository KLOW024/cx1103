#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*qn1
    int studentNo = 0, mark;
    printf(" student_id:\n ");
    scanf("%d", &studentNo);
    while(studentNo != -1){
    printf(" Enter_mark :\n");
    scanf("%d", &mark);
    switch(mark){
        case 75 ... 100 :
            printf("marks is A");
            break;
        case 65 ... 74 :
            printf("marks is B");
            break;
        case 55 ... 64 :
            printf("marks is C");
            break;
        case 45 ... 54 :
            printf("Marks is D");
            break;
        default :
            printf("Failed");
            break;

    }
    printf(" student_id:\n ");
    scanf("%d", &studentNo);
    scanf("%c")
    }
    */
    //qn2
    int nOL, cL=0, vaL = 0, sm = 0, i;
    printf("enter nOL \n");
    scanf("%d", &nOL);
    for(i=1; i <=nOL; i++){
            printf("enter vaL \n");
            scanf("%d", &vaL);
            while(vaL != -1){
                    sm += vaL;
                    cL ++;
                    scanf("%d", &vaL);
                    }
            float ans = (float)sm/(float)cL ;
            printf("%.2f\n", ans);
            vaL = sm = cL = 0;

    }
    //qn3
    int i, d, j, k;
    printf("enter range :");
    scanf("%d", &d);
    for(j =1; j <=d; j++){
        k = (j%3 != 0) ? j%3 : 3;
        for(i=1; i<=j; i++){
        printf("%d", k);

    }printf("\n");

    }
    return 0;
}
